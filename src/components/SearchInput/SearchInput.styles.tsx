import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
    backgroundColor: '#fff',
    height: 48,
    paddingHorizontal: 8,
    marginTop: 15,
  },
  textInputStyle: {
    width: '100%',
  },
});
